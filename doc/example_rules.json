{
  "RULESAuth": [
    {
      "EventLimit": 7,
      "IsEnabled": 1,
      "Message": "Over 7 pam auth failure events in 10 minutes",
      "OutTable": "SIEMAuth",
      "RuleName": "PAM_AuthFail_10m",
      "SQLQuery": "SELECT Id FROM Auth WHERE DateStamp >= timestamp(date_sub(now(), interval 10 minute)) AND (Message LIKE \"%fail%\" AND Message LIKE \"%pam%\")",
      "Severity": 4,
      "SourceTable": "Auth",
      "TimeInt": 10
    },
    {
      "EventLimit": 2,
      "IsEnabled": 1,
      "Message": "Over 2 pam auth failure events for root in 10 minutes",
      "OutTable": "SIEMAuth",
      "RuleName": "PAM_RootFail_10m",
      "SQLQuery": "SELECT Id FROM Auth WHERE DateStamp >= timestamp(date_sub(now(), interval 10 minute)) AND (Message LIKE \"%fail%\" AND Message LIKE \"%pam%\" AND Message LIKE \"%root%\")",
      "Severity": 2,
      "SourceTable": "Auth",
      "TimeInt": 10
    }
  ],
  "RULESNight": [
    {
      "EventLimit": 0,
      "IsEnabled": 1,
      "Message": "Over 0 motion events in one hour at night",
      "OutTable": "SIEMVis",
      "RuleName": "VIS_NightMoves",
      "SQLQuery": "SELECT Id FROM Visual WHERE DateStamp >= timestamp(date_sub(now(), interval 1 hour)) AND (Message LIKE \"%PIR: Motion%\")",
      "Severity": 4,
      "SourceTable": "Auth",
      "TimeInt": 0
    }
  ]
}
